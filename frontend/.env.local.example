# Copy to .env.local for local development.

# Base URL of the Spring Boot backend. The NEXT_PUBLIC_ prefix is required because
# the API client runs in client components, not only on the server.
NEXT_PUBLIC_API_URL=http://localhost:8080
